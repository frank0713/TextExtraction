import wx

class TextMainForm(wx.Frame):
    # 寻找框1
    # 寻找按钮1
    # 文件选择框1
    # 文件选择按钮1
    # 文件排序按钮4个，按时间排序，按匹配度排序，按大小排序，默认排序
    # 左侧文件展示框1（大）
    # 右侧文件内容显示框1（小）
    def __init__(self, parent, id=ID_ANY, title=EmptyString, pos=DefaultPosition, size=DefaultSize, style=DEFAULT_FRAME_STYLE, name=FrameNameStr):
        pass